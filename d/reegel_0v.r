i e C # { stem_transform = prerule && stem_grammar = 0v: stem_transform = postrule }
u e C # { stem_transform = prerule && stem_grammar = 0v: stem_transform = postrule }
e i C # { stem_transform = prerule && stem_grammar = 0v: stem_transform = postrule }
a u Wi # { stem_transform = prerule && stem_grammar = 0v: stem_transform = postrule }
a u YC # { stem_transform = prerule && stem_grammar = 0v: stem_transform = postrule }
a u YCC # { stem_transform = prerule && stem_grammar = 0v: stem_transform = postrule }
a u YCCC # { stem_transform = prerule && stem_grammar = 0v: stem_transform = postrule }
a i C # { stem_transform = prerule && stem_grammar = 0v: stem_transform = postrule }
